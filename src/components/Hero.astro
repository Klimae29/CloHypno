---
// Hero.astro
---

<section id="accueil" class="min-h-screen flex items-center justify-center bg-gradient-to-br from-neutral-50 to-primary-50 pt-20">
  <div class="container mx-auto px-6 py-12">
    <h1 class="text-4xl md:text-5xl lg:text-6xl font-lora font-bold text-primary-700 mb-6 leading-tight text-center">
      Retrouvez votre <br class="block md:hidden" />
        <span class="text-accent-500">équilibre</span>
      grâce à l'hypnose
    </h1>
    <h1 class="flex flex-col items-center text-4xl md:text-5xl lg:text-6xl font-lora font-bold text-primary-700 mb-6 leading-tight text-center">
      <img src="/images/logodouarnenez2.png" alt="Logo Douarnenez" class="mt-4 w-24 md:w-32 lg:w-40 h-auto">
    </h1>
    <br>
    <br>
    <div class="grid lg:grid-cols-2 gap-12 items-center">

      <!-- Contenu texte -->
      <div class="text-center lg:text-left" data-aos="fade-up">

        <p class="text-lg md:text-xl text-text-light mb-8 max-w-2xl mx-auto lg:mx-0">
          Bienvenue sur <strong>Avelenn</strong>, le site de votre hypnothérapeute.
          Accompagnement bienveillant pour surmonter vos difficultés et retrouver votre bien-être.
        </p>

        <div class="flex flex-col sm:flex-row gap-4 justify-center lg:justify-start">
          <a href="#hypnose" class="bg-primary-500 text-white px-8 py-4 rounded-full hover:bg-primary-600 transition-all duration-300 font-semibold shadow-lg hover:shadow-xl transform hover:-translate-y-1">
            Découvrir l'hypnose
          </a>
          <a href="#contact" class="border-2 border-primary-500 text-primary-500 px-8 py-4 rounded-full hover:bg-primary-500 hover:text-white transition-all duration-300 font-semibold">
            Prendre rendez-vous
          </a>
        </div>
      </div>

      <!-- Vidéo -->
      <div class="relative" data-aos="fade-left" data-aos-delay="200">
        <div class="relative z-10 bg-white rounded-2xl shadow-2xl overflow-hidden aspect-video">
          <video
            src="/videos/video_douarnenez.mp4"
            autoplay
            muted
            loop
            playsinline
            class="w-full h-full object-cover"
          ></video>
        </div>

        <!-- Éléments décoratifs -->
        <div class="absolute -top-6 -right-6 w-24 h-24 bg-accent-200 rounded-full opacity-60 animate-float"></div>
        <div class="absolute -bottom-6 -left-6 w-32 h-32 bg-secondary-200 rounded-full opacity-40 animate-float" style="animation-delay: 1s;"></div>
      </div>

    </div>

    <!-- Indicateur de scroll -->
    <!-- <div class="absolute bottom-8 left-1/2 transform -translate-x-1/2 animate-bounce">
      <a href="#hypnose" class="text-primary-500 hover:text-primary-600 transition-colors">
        <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 14l-7 7m0 0l-7-7m7 7V3"></path>
        </svg>
      </a>
    </div> -->
    <div class="mouse-wheel" aria-hidden="true">
      <div class="wheel"></div>
    </div>
  </div>
</section>

<style>
  @keyframes float {
    0%, 100% { transform: translateY(0px); }
    50% { transform: translateY(-20px); }
  }

  .animate-float {
    animation: float 3s ease-in-out infinite;
  }

  /* --- Indicateur de défilement stylisé : Souris ---*/
.mouse-wheel {
  position: fixed;
  bottom: 30px;
  left: 50%;
  transform: translateX(-50%);
  width: 30px;
  height: 50px;
  border: 2px solid #4A664A;;
  border-radius: 15px;
  display: flex;
  justify-content: center;
  align-items: start;
  opacity: 0.7;
  transition: opacity 0.3s ease;
  pointer-events: none;
}

.mouse-wheel:hover {
  opacity: 1;
}

.wheel {
  width: 4px;
  height: 10px;
  background-color: #4A664A;
  border-radius: 2px;
  margin-top: 10px;
  animation: scrollWheel 1.5s infinite;
}

/* Animation de la molette */
@keyframes scrollWheel {
  0% {
    transform: translateY(0);
    opacity: 1;
  }
  100% {
    transform: translateY(20px);
    opacity: 0;
  }
}

/* Accessibilité : réduire les animations si demandé */
@media (prefers-reduced-motion: reduce) {
  .wheel {
    animation: none;
  }
}
</style>
